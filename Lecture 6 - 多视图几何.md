# Lecture6 多视图几何 / 立体视觉 笔记

> 核心主题：如何从两张或多张图像中恢复三维信息

---

# 1. 本章整体框架

多视图几何研究的是：

- 多个视角之间的几何关系
- 如何利用两幅或多幅图像恢复场景的三维结构

本章主线：

1. 相机模型
2. 相机标定
3. 极线几何
4. 本征矩阵 E 与基础矩阵 F
5. E / F 的估计
6. 三角测量
7. 应用：SfM、运动估计、增强现实

---

# 2. 多视图重建的基本思想

多视图重建（Multi-view reconstruction）：

- 从两张或多张图像恢复物体/场景的三维形状
- 本质依赖：
  - 图像间对应点匹配
  - 相机之间的相对几何关系
  - 三角测量

核心目标：

- 已知两幅图中的对应点
- 求相机之间的关系
- 恢复三维点坐标

---

# 3. 相机模型

## 3.1 相机内参（Intrinsic Parameters）

内参描述相机自身成像特性，常写成矩阵：

\[
K =
\begin{bmatrix}
f_x & s & u_c \\
0 & f_y & v_c \\
0 & 0 & 1
\end{bmatrix}
\]

其中：

- \(f_x, f_y\)：x、y方向焦距
- \(u_c, v_c\)：主点坐标
- \(s\)：斜切系数（skew）

特点：

- \(K\) 是一个 3×3 上三角矩阵
- 有 5 个自由度

内参的作用：

- 将相机坐标系中的点映射到图像像素坐标系

---

## 3.2 相机外参（Extrinsic Parameters）

外参描述相机在世界中的位置和姿态：

- \(R\)：旋转矩阵
- \(T\)：平移向量

作用：

- 将世界坐标系中的三维点变换到相机坐标系中

---

## 3.3 投影矩阵

三维点到图像点的完整映射：

\[
P = K[R|T]
\]

其中：

- \(K\)：内参
- \([R|T]\)：外参
- \(P\)：投影矩阵

意义：

- 世界坐标中的三维点 \((X,Y,Z)\)
- 经过投影矩阵映射到图像中的二维点 \((u,v)\)

---

# 4. 相机标定（Camera Calibration）

## 4.1 标定的目标

相机标定就是求：

- 投影矩阵 \(P\)
- 进一步分解得到 \(K, R, T\)

---

## 4.2 基本思路

已知：

- 一组三维世界点坐标 \(\{\tilde X_i\}\)
- 它们在图像中的对应点 \(\{\tilde p_i\}\)

通过这些 3D-2D 对应关系：

1. 先估计投影矩阵 \(P\)
2. 再分解得到相机内参与外参

常用标定物：

- 棋盘格

---

## 4.3 投影矩阵 \(P\) 的估计

投影矩阵 \(P\)：

- 共有 12 个元素
- 因为齐次坐标下整体尺度无关，所以只有 11 个自由度

估计方法：

- 每组 3D-2D 对应点提供约束
- 至少需要 6 组以上对应点
- 实际常用最小二乘法求解

---

## 4.4 从 \(P\) 分解出 \(K, R, T\)

已知：

\[
P = K[R|T]
\]

利用以下先验：

- \(K\) 是上三角矩阵
- \(R\) 是正交矩阵

可通过矩阵分解从 \(P\) 中恢复：

- 相机内参 \(K\)
- 旋转矩阵 \(R\)
- 平移向量 \(T\)

---

# 5. 归一化相机（Calibrated Camera）

如果相机内参 \(K\) 已知，可以把图像点变换到归一化坐标系：

\[
\tilde x = K^{-1}x
\]

则投影模型变成：

\[
\tilde P = [R|T]
\]

此时：

- 内参矩阵相当于单位矩阵
- 图像坐标不再受内参影响
- 后续几何关系只与相机外参有关

归一化相机的意义：

- 将问题简化
- 为本征矩阵 \(E\) 的定义做准备

---

# 6. 极线几何（Epipolar Geometry）

极线几何描述的是：

- 两台相机拍摄同一三维点时
- 两幅图中的对应点之间满足的几何约束

---

## 6.1 极线约束的核心思想

设右图中有一点 \(p_2\)，它在左图中的对应点 \(p_1\)：

- 不需要在整张左图中搜索
- 只可能位于左图中的一条特定直线上

这条直线就是：

- 极线（Epipolar line）

意义：

- 将二维搜索变成一维搜索
- 大幅减少匹配范围

---

## 6.2 极平面、极线、极点

对于一个三维点 \(P\)：

- 它与两个相机光心 \(C_1, C_2\) 共同确定一个平面
- 这个平面叫做极平面（Epipolar plane）

极平面与左右图像平面的交线分别是：

- 左图极线 \(l_1\)
- 右图极线 \(l_2\)

极点（Epipole）：

- 一个相机中心在另一幅图像中的投影点
- 所有极线都经过极点

---

# 7. 本征矩阵 E（Essential Matrix）

## 7.1 定义

在已知相机内参、使用归一化坐标时，对应点满足：

\[
x_2^T E x_1 = 0
\]

其中：

- \(x_1, x_2\)：归一化坐标下的对应点
- \(E\)：本征矩阵

这个公式称为：

- 极线约束方程

---

## 7.2 几何意义

已知一幅图中的点，可通过 \(E\) 计算另一幅图中的极线。

例如：

- 给定 \(x_1\)
- 可求右图极线 \(l_2 = E x_1\)

则右图对应点 \(x_2\) 必须满足：

\[
x_2^T l_2 = 0
\]

即：

\[
x_2^T E x_1 = 0
\]

---

## 7.3 本征矩阵的来源

假设第一台相机作为参考相机：

\[
P_1 = [I|0], \quad P_2 = [R|T]
\]

则有：

\[
E = [T]_\times R
\]

其中：

- \(R\)：相机间相对旋转
- \(T\)：相机间相对平移
- \([T]_\times\)：由向量 \(T\) 构造出的反对称矩阵，用来表示叉乘

所以：

- 本征矩阵编码了两台已标定相机的相对位姿关系

---

## 7.4 本征矩阵的自由度

- \(R\)：3 个自由度
- \(T\)：3 个自由度
- 但平移尺度无关，因此减去 1 个自由度

所以：

- \(E\) 有 5 个自由度

---

# 8. 基础矩阵 F（Fundamental Matrix）

## 8.1 定义

对于未归一化的原始像素坐标，对应点满足：

\[
x_2^T F x_1 = 0
\]

其中：

- \(F\)：基础矩阵

---

## 8.2 E 与 F 的关系

归一化坐标与像素坐标关系：

\[
\tilde x = K^{-1}x
\]

由此可推出：

\[
F = K^{-T} E K^{-1}
\]

所以：

- \(E\)：用于归一化相机
- \(F\)：用于原始像素坐标

---

## 8.3 E 与 F 的区别

### 本征矩阵 E

- 适用于已校正相机
- 使用归一化坐标
- 与相机相对位姿直接相关

### 基础矩阵 F

- 适用于未校正相机
- 使用原始图像坐标
- 更常见、更通用

---

## 8.4 基础矩阵 F 的性质

1. 若 \(F\) 是基础矩阵，则 \(kF\) 也是基础矩阵
2. \(rank(F)=2\)
3. \(F\) 有 7 个自由度

原因：

- 3×3 矩阵有 9 个参数
- 去掉整体尺度自由度 1 个
- 再满足秩为 2 的约束，减去 1 个

因此总自由度为 7

---

# 9. 8点算法（8-point algorithm）

## 9.1 基本思想

假设有 \(N \ge 8\) 对匹配点：

\[
x_i \leftrightarrow \bar x_i
\]

它们满足：

\[
\bar x_i^T F x_i = 0
\]

对每一对匹配点，都能展开成一个关于 \(F\) 九个元素的线性方程。

---

## 9.2 为什么叫 8 点算法

- 一对对应点提供一个线性方程
- \(F\) 有 9 个元素，但整体尺度无关
- 因此至少需要 8 对点来确定 \(F\)

若相机已标定，则同理可以估计 \(E\)

---

## 9.3 矩阵形式

将所有点对约束写成矩阵形式：

\[
Af = 0
\]

其中：

- \(A\)：由所有匹配点构成的矩阵
- \(f\)：将 \(F\) 的 9 个元素排成列向量

本质：

- 几何约束转化成线性代数问题

---

# 10. 由 E 恢复相机姿态

如果已经得到本征矩阵 \(E\)，可以进一步恢复：

- 相机之间的旋转 \(R\)
- 相机之间的平移 \(T\)

通常做法：

- 设第一台相机为参考坐标系
- \(P_1 = [I|0]\)
- 通过 SVD 分解 \(E\)，恢复第二台相机：
  \[
  P_2 = [R|T]
  \]

---

# 11. 三角测量（Triangulation）

## 11.1 问题定义

已知：

- 两台相机投影矩阵 \(P_1, P_2\)
- 两幅图中的一对匹配点 \(x_1, x_2\)

求：

- 三维点 \(X\)

---

## 11.2 几何直觉

一个图像点对应一条从相机光心出发的射线。

同一个三维点在两幅图中的对应点：

- 分别确定两条射线
- 它们的交点就是三维点位置

这就是三角测量

---

## 11.3 实际情况

由于噪声存在：

- 两条射线通常不会严格相交
- 实际中通常求最优意义下最接近交点的位置

---

# 12. 多视图几何的应用

## 12.1 Structure from Motion（SfM）

从相机运动中恢复三维结构

基本流程：

1. 前两张图：
   - 估计 \(E\)
   - 恢复相机位姿
   - 三角测量得到一批三维点

2. 新图像加入：
   - 利用已重建三维点估计新相机位姿
   - 再恢复更多三维点

3. 重复迭代

作用：

- 三维建模
- 无人机测绘
- 视觉 SLAM

---

## 12.2 稠密三维重建

除了恢复稀疏特征点，还可以：

- 恢复稠密深度图
- 恢复完整表面
- 进行体素重建

---

## 12.3 刚体运动估计

如果场景中存在多个独立运动物体：

- 不同物体对应不同的相对运动
- 因此可能存在多个基础矩阵 \(F\)

应用：

- 运动分割
- 多刚体运动分析

---

## 12.4 增强现实（AR）

若已知：

- 相机内参 \(K\)
- 相机位置与朝向

则可将虚拟三维物体正确叠加到真实图像中。

本质：

- 利用真实相机的几何参数，正确投影虚拟物体

---

# 13. 本章公式总结

## 投影矩阵

\[
P = K[R|T]
\]

---

## 归一化坐标

\[
\tilde x = K^{-1}x
\]

---

## 本征矩阵约束

\[
x_2^T E x_1 = 0
\]

---

## 本征矩阵表达式

\[
E = [T]_\times R
\]

---

## 基础矩阵约束

\[
x_2^T F x_1 = 0
\]

---

## E 与 F 的关系

\[
F = K^{-T} E K^{-1}
\]

---

# 14. 易混点总结

## 14.1 内参 vs 外参

### 内参 \(K\)
- 描述相机自身成像性质
- 与相机位置无关

### 外参 \(R,T\)
- 描述相机在世界中的位置和方向
- 与相机姿态有关

---

## 14.2 本征矩阵 E vs 基础矩阵 F

### E
- 已知内参
- 归一化坐标
- 5 自由度

### F
- 未知内参也可用
- 原始像素坐标
- 7 自由度

---

## 14.3 极线几何的本质

不是任意匹配点都行，而是：

- 同一三维点在两幅图中的投影
- 必须满足极线约束

因此：

- 一幅图中的点，在另一幅图中的对应点只可能落在一条极线上

---

## 14.4 三角测量的前提

三角测量必须已知：

- 两个视角
- 相机关系
- 匹配点

不能仅靠单张图像直接恢复真实三维坐标

---

# 15. 本章小结

本章核心内容可以概括为：

1. 用投影矩阵描述三维点到二维图像的映射
2. 通过标定求得相机内参与外参
3. 利用极线几何约束对应点关系
4. 用本征矩阵 \(E\) 和基础矩阵 \(F\) 表达这种约束
5. 通过 8 点算法估计 \(E/F\)
6. 利用三角测量恢复三维结构
7. 应用于三维重建、运动估计和增强现实等任务

一句话总结：

**多视图几何研究的是：如何利用多张图像之间的对应关系与几何约束，恢复相机运动和场景三维结构。**